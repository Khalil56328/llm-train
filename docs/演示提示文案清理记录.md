# 前端「演示版」提示文案清理记录

> 清理日期：2026-08-31
> 范围：`web-ui/src` 中页面上向用户展示的演示/降级相关文字提示（el-alert、form-tip、field-tip、步骤条 desc、页头 desc 等）。
> 目的：去除界面上"演示版 / 实际按 XX 执行 / 仅展示"类文案，使各功能页面呈现统一的正式表述。

---

## 一、修改清单

### 1. 偏好对齐列表页 `src/views/training/AlignmentList.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 页头 desc | 平台提供 DPO/KTO/ORPO/SimPO 等离线对齐方法**（演示版暂不支持 RLHF/PPO）**。 | 平台提供 DPO/KTO/ORPO/SimPO 等离线对齐方法。 |

### 2. 创建对齐任务页 `src/views/training/AlignmentCreate.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| RLHF 方法提示（form-tip，整行删除） | RLHF(PPO) 需要 Reward Model 与多卡推理引擎；演示环境将按 DPO 算法实际执行训练，任务信息与引擎命令仍按 RLHF 展示。 | （已删除） |
| 偏好数据集提示 | 偏好数据集需为偏好对格式（每行含 chosen / rejected 字段**，可用演示数据集 preference_demo**）；… | 偏好数据集需为偏好对格式（每行含 chosen / rejected 字段）；… |
| Step 4 代码注释 | `<!-- Step 4: 资源配置（演示版仅展示，实际执行按宿主机真实资源） -->` | `<!-- Step 4: 资源配置 -->` |
| 步骤条 Step 4 desc | 资源参数仅展示，按宿主机真实资源执行 | 选择资源池，配置资源规格 |

### 3. 创建场景训练任务页 `src/views/training/SceneTrainCreate.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 页头 desc | …一键启动训练**（演示版：多阶段流程已简化为单阶段 SFT）**。 | …一键启动训练。 |
| Step 4 代码注释 | `<!-- Step 4: 资源配置（演示版仅展示，实际执行按宿主机真实资源） -->` | `<!-- Step 4: 资源配置 -->` |
| 步骤条 Step 4 desc | 资源参数仅展示，按宿主机真实资源执行 | 选择资源池，配置资源规格 |
| OCR 场景数据集提示 | …多模态 SFT 数据集**，如内置演示数据集 scene_ocr_demo（票据/单据/文档识别问答）**。 | …多模态 SFT 数据集。 |
| 视觉理解场景数据集提示 | …多模态 SFT 数据集**，如内置演示数据集 scene_vision_demo（图像描述 / VQA 问答）**。 | …多模态 SFT 数据集。 |
| 客服场景数据集提示 | …请选择对话式 SFT 数据集**，如内置演示数据集 sft_self_cognition 或 scene_customer_service（电商客服多轮对话）**。 | …请选择对话式 SFT 数据集。 |

### 4. 创建压缩训练任务页 `src/views/training/CompressionCreate.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 页头 desc | …提升部署效率**（演示版仅支持量化，剪枝/蒸馏暂不提供）**。 | …提升部署效率。 |
| 量化校准提示 | GPTQ / AWQ 量化需要校准数据集**（可选用演示数据集 sft_self_cognition）**。 | GPTQ / AWQ 量化需要校准数据集。 |
| Step 3 代码注释 | `<!-- Step 3: 资源配置（演示版仅展示，实际执行按宿主机真实资源） -->` | `<!-- Step 3: 资源配置 -->` |
| 步骤条 Step 3 desc | 资源参数仅展示，按宿主机真实资源执行 | 选择资源池，配置资源规格 |

### 5. 创建预训练任务页 `src/views/training/PreTrainCreate.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 预训练数据集提示 | 预训练数据集需为纯文本格式（JSONL 每行 {"text": "..."}**，可用演示数据集 pretrain_demo**）；… | 预训练数据集需为纯文本格式（JSONL 每行 {"text": "..."}）；… |

### 6. 算子版本创建页 `src/views/operator/OperatorVersionCreate.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 基础镜像 field-tip（整行删除） | 该字段为声明/展示用：任务在宿主机直接执行（TRAIN_CONTAINER_RUNTIME=local），实际运行环境为宿主机镜像，如 ModelScope Notebook 的 ubuntu22.04-cuda12.8.1-py312-torch2.10.0-1.39.0 | （已删除） |

### 7. 算子版本编辑页 `src/views/operator/VersionEdit.vue`

| 位置 | 修改前 | 修改后 |
|---|---|---|
| 基础镜像 field-tip（整行删除） | 同上 | （已删除） |

---

## 二、本次未变更的运行时行为（重要）

本次仅删除 UI 文案，**以下后端实际行为保持不变**，相关方需知悉（提示已从页面移除，但功能降级逻辑仍在）：

| 功能 | 实际行为（代码位置） |
|---|---|
| RLHF/PPO 对齐训练 | 无多卡资源时后端兜底按 DPO 执行，任务信息与引擎命令仍按 RLHF 展示（`backend/app/tasks/executor.py` RLHF→DPO 兜底分支） |
| 场景训练多阶段流程 | 简化为单阶段 SFT 执行 |
| 模型压缩 | 量化（GPTQ/AWQ/BnB/GGUF）有真实执行链路；剪枝/蒸馏类型字段正确入库并在列表/详情/编辑页精确回显，底层统一按量化导出执行（2026-08-31 已改造，参考对齐 RLHF→DPO 兜底模式） |
| 训练资源配置（资源池/GPU/CPU/内存） | 仅入库展示，执行时按宿主机真实资源，固定单卡 `CUDA_VISIBLE_DEVICES=0` |
| 算子镜像字段 | 仅作为声明/展示，不参与任务调度（标准部署下训练以宿主机引擎执行） |
| 内置演示数据集 | `pretrain_demo` / `preference_demo` / `scene_ocr_demo` / `scene_vision_demo` / `sft_self_cognition` / `scene_customer_service` 仍存在于数据集列表，可正常选用；页面仅不再主动引导 |

---

## 三、验证结果

- 全局搜索 `演示` / `仅展示，按宿主机` / `声明/展示用`：`web-ui/src` 下 0 残留；
- 7 个修改文件 ESLint 检查通过，无新增错误；
- 功能逻辑（表单校验、提交参数、步骤流转）零改动，仅文案变更。
